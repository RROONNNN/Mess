import 'package:flutter/material.dart';

class cuocgoi extends StatelessWidget {
  const cuocgoi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Text('cuoc goi'),
    );
  }
}