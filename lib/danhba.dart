import 'package:flutter/material.dart';

class danhba extends StatelessWidget {
  const danhba({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Text('danh ba'),
    );
  }
}